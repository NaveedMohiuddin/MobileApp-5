import 'package:flutter/material.dart';

class AppTheme {
  static ButtonStyle flatButtonStyle = TextButton.styleFrom(
    foregroundColor: Colors.white,
    backgroundColor: const Color.fromARGB(255, 60, 78, 247), // Background Color
  );
}
